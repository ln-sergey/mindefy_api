{
  "name": "@shared-tools/http",
  "version": "0.1.0",
  "main": "lib/index.js",
  "typings": "types/index.d.ts",
  "author": "ln-sergey",
  "files": [
    "lib",
    "types"
  ],
  "scripts": {
    "upd": "npm run build && npm link",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf lib",
    "compile": "tsc"
  },
  "dependencies": {
    "koa": "^2.14.2",
    "koa-router": "^12.0.0"
  },
  "devDependencies": {
    "@types/koa": "^2.13.8",
    "@types/koa-router": "^7.4.4",
    "typescript": "^4.9.5"
  }
}
